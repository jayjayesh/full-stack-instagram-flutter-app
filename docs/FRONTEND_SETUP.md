# Frontend Setup

Run:

```bash
cd frontend/instagramflutterapp
flutter pub get
flutter run
```

The app reads `API_BASE_URL` from `.env`.

Use `http://localhost:3000/api` for macOS/iOS simulator.

Use `http://10.0.2.2:3000/api` for Android emulator.
